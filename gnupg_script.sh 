sudo apt update 
# updates the list of available packages in the Linux repositories 

sudo apt upgrade 
# upgrades the packages configured in the system 

sudo apt install parted
# installs parted on Debian 

sudo parted -l && echo -e "\n--\n" && lsblk -f && echo -e "\n--\n"
parted -l
# shows the partition tables 

lsblk -f 
# displays the disk structure with its logical volumes 

&&
# executes the next command only if the previous one was successful 

"\n--\n"
# line break
