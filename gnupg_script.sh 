ls -a
show the files wiht a short form 
la --all 
long form 
 ls -l -a -h 
 list all files and permissions 
 ls -l 
 list the files but dont shows the hide files 
ls -lah
list the files but whith a simplify form 
mkdir -- -rf 
create a folder called -rf 
rmdir -- -rf 
delete the folder 
ls --help 
show a short manual 
man ls 
show the complete manual 
n
go rigth 
N 
Go back 
 #--depth <depth>
           #Create a shallow clone with a history truncated to the specified number of commits. Implies --single-branch unless --no-single-branch is given to fetch the
           #histories near the tips of all branches. If you want to clone submodules shallowly, also pass --shallow-submodules.

ls -l 
#ls=list 
#-=type of file 
#symbolich link 
chmod +x script.sh 
#everyone can execute 
chmod u+x script.sh 
#only the owner can execute 
chmod o-r secreto.txt 
#remove read to others 
chmod u+rw,go-rwx privado 
#nobody can do anything 
sudo echo "hola" > /etc/archivo_protegido
#give permissions in echo but in etc dont give permissions 
echo "hola" | sudo tee /etc/archivo_protegido > /dev/null
#send hola to a black hole for dont show the word in the terminal 
sudo sh -c 'echo "chao" >> /etc/archivo_protegido'
#do a entire shell 
sudo -i 
#go to root have permissions but not forever 
echo "$HOME"
#Expand the variable
echo '$HOME'
#show the string 
      ls -l hola-sh 
      ls -l hola.sh 
     chmod +x hola.sh 
     ls -l hola.sh 
   ./hola.sh 
The issues are problems in the code with the solutions of the ingeniers 
stack overflow have problems in the real work of programmer, ia can not solve this porblms because dosent has the operience of a real work 
umask 027 sets the default file permissions so new files and folders are not accessible to others and only the owner and group have limited access.

umask shows the current default permission mask for newly created files and directories.

touch archivo2 creates a new empty file called archivo2 or updates its modification date if it already exists.

mkdir directorio2 creates a new directory called directorio2.

ls -l lists files and directories in long format, showing details like permissions, owner, size, and modification date.